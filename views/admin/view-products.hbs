<section>
    <div class="container">
        <div class="row">
            <a class="ml-auto" href="/admin/add-products"><button class="btn btn-success mt-3">Add Products</button></a>
        </div>
        <table class="table mt-3" id="myTable">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Product Name</th>
      <th scope="col">Category</th>
      <th scope="col">Description</th>
      <th scope="col">Price</th>
      <th scope="col">Image</th>
      <th scope="col">options</th>
    </tr>
  </thead>
  <tbody>
    
      {{#each products}}
      <tr>
      <th scope="row">{{index}}</th>
      <td>{{this.product-name}}</td>
      <td>{{this.product-category}}</td>
      <td>{{this.product-description}}</td>
      <td>{{this.product-price}}</td>
      <td><img src="/product-images/{{this._id}}.jpg" height="60px" width="60px"></td>
      <td><a href="/admin/edit-product?id={{this._id}}" class="btn btn-primary">edit</a><a href="/admin/delete-product?id={{this._id}}" class="btn btn-danger" onclick="return confirm('are you want to delete {{this.product-name}}')">delete</a></td>
       </tr>
      {{/each}}
   
   
  </tbody>
</table>
    </div>
</section>

<script>
  
1
2
3
$(document).ready( function () {
    $('#myTable').DataTable();
} );
</script>